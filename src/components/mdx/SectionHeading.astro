---
// SectionHeading.astro
const { children, color = "#6366f1" } = Astro.props;
---
<div class="section-heading" style={`background:${color}`}>
  <span>{children}</span>
</div>

<style>
.section-heading {
  display: inline-block;
  padding: 0.5rem 2rem;
  border-radius: 2rem;
  color: #fff;
  font-size: 2rem;
  font-weight: bold;
  margin-bottom: 2rem;
  letter-spacing: 0.01em;
}
</style>
